<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.hypernova.permission.CONTROL_COCKPIT_APPS"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.hypernova.permission.CONTROL_COCKPIT_APPS" />

    <application
        android:allowBackup="false"
        android:hasCode="true"
        android:label="HyperNova Vehicle Gateway"
        android:supportsRtl="true">

        <service
            android:name=".VehicleGatewayService"
            android:exported="true"
            android:permission="com.hypernova.permission.CONTROL_COCKPIT_APPS"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.hypernova.vehiclegateway.action.BIND_GATEWAY" />
            </intent-filter>
        </service>

    </application>
</manifest>
