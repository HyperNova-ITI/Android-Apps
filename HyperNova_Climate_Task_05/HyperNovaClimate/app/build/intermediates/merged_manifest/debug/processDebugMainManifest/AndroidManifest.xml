<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hypernova.climate.dev"
    android:versionCode="1"
    android:versionName="1.0-dev" >

    <uses-sdk
        android:minSdkVersion="35"
        android:targetSdkVersion="36" />

    <!-- Designed for Android Automotive OS. -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />

    <!--
        Vehicle climate access. Exact permissions depend on the target AOSP
        image and the active backend (VHAL vs direct gateway). Declared here
        for the CarProperty/VHAL path; harmless when the Ethernet backend is
        selected. INTERNET is required by the direct-Ethernet backend.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_CLIMATE" />
    <uses-permission android:name="android.car.permission.CAR_INFO" />

    <!--
        Signature-level permission protecting the NOVA command service.
        (Service itself is added in the IPC phase; UI phase declares the
        permission so the contract is visible.)
    -->
    <permission
        android:name="com.hypernova.permission.CONTROL_COCKPIT_APPS"
        android:protectionLevel="signature" />
    <permission
        android:name="com.hypernova.climate.dev.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.hypernova.climate.dev.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.HyperNovaClimate" >
        <activity
            android:name="com.hypernova.climate.ClimateActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="false"
            android:screenOrientation="portrait" >

            <!-- Launcher entry for development / adb. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Cockpit open action used by the HyperNova Launcher. -->
            <intent-filter>
                <action android:name="com.hypernova.climate.action.OPEN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            The NOVA command service (ClimateCommandService) and the read-only
            ClimateStatusService are registered in the IPC integration phase.
        -->

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.hypernova.climate.dev.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>