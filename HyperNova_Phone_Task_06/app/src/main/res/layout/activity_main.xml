<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hn_background_primary">

    <!-- =========================================================
         HEADER
         ========================================================= -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="0dp"
        android:layout_height="64dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!--
             Internal Phone navigation.

             This returns from Contacts / Recents / Keypad / Devices
             to the Phone HOME screen.
        -->
        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/cd_back"
            android:padding="12dp"
            android:src="@drawable/ic_back" />

        <!--
             Automotive HOME.

             This is deliberately separate from navHome below.

             navHome:
                 Phone screen -> Phone HOME.

             iviHomeButton:
                 Phone app -> Android HOME -> HyperNova Launcher.
        -->
        <Button
            android:id="@+id/iviHomeButton"
            android:layout_width="88dp"
            android:layout_height="44dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/bg_outline_button"
            android:contentDescription="@string/cd_home"
            android:gravity="center"
            android:minWidth="0dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:text="HOME"
            android:textAllCaps="true"
            android:textColor="@color/hn_primary_cyan"
            android:textSize="12sp"
            android:textStyle="bold" />

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:contentDescription="@string/cd_phone"
            android:src="@drawable/ic_phone" />

        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/hn_text_primary"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/headerStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxEms="18"
            android:maxLines="1"
            android:textColor="@color/hn_text_secondary"
            android:textSize="11sp" />

    </LinearLayout>


    <!-- =========================================================
         SCREEN CONTENT
         ========================================================= -->
    <FrameLayout
        android:id="@+id/content"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/bottomNavigation"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/header" />


    <!-- =========================================================
         PHONE INTERNAL NAVIGATION
         ========================================================= -->
    <LinearLayout
        android:id="@+id/bottomNavigation"
        android:layout_width="0dp"
        android:layout_height="76dp"
        android:background="@drawable/bg_bottom_nav"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/navHome"
            style="@style/Widget.HyperNova.Nav"
            android:contentDescription="@string/cd_home"
            android:text="Home" />

        <Button
            android:id="@+id/navKeypad"
            style="@style/Widget.HyperNova.Nav"
            android:contentDescription="@string/cd_keypad"
            android:text="Keypad" />

        <Button
            android:id="@+id/navContacts"
            style="@style/Widget.HyperNova.Nav"
            android:contentDescription="@string/cd_contacts"
            android:text="Contacts" />

        <Button
            android:id="@+id/navRecents"
            style="@style/Widget.HyperNova.Nav"
            android:contentDescription="@string/cd_recents"
            android:text="Recents" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
